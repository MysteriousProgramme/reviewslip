{
  "name": "baanpong-review-app",
  "version": "1.0.0",
  "private": true,
  "description": "Guest review helper for Baanpong Lodge — generates a short 5-star review, copies it, and sends the guest to Google.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "import:sqlite": "node scripts/import-sqlite.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "better-sqlite3": "^11.3.0"
  }
}
